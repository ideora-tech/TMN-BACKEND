<?php

use Illuminate\Support\Facades\Route;

Route::prefix('v1')->group(function () {
    // Auth routes — loaded by AuthServiceProvider
    // All other module routes loaded by their ServiceProviders
});
